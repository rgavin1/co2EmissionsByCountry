export type Country = {
    id: string;
    entity: string;
    code: string;
    year: string;
    annual_co2_emissions: number;
    // population: string;
    // population: string;
    flag: string;
}